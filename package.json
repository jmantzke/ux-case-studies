{
  "name": "design-tokens",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "sass style.scss style.css --no-source-map",
    "watch:css": "sass style.scss style.css --no-source-map --watch",
    "deploy": "rsync -avz --progress -e \"ssh -p 2022\" ./ enfineitz@enfineitz.com:~/publicsubs_case_studies/case-studies.enfineitz.com/",
    "validate:schema": "ajv validate -s token-schema.json -d primitives.json",
    "validate:refs": "node scripts/resolve-refs.js primitives.json semantic/grid.json semantic/type.json",
    "validate": "npm run validate:schema && npm run validate:refs"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0"
  }
}
